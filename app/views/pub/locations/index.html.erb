<% content_for :title, "Locations" %>

<%= render 'pub/shared/search_block' %>

<div class="bg-white p-6 rounded-lg shadow-md">
  <div class="mb-6">
    <%= render 'pub/shared/breadcrumbs', crumbs: [
      { title: "Lore" },
      { title: "Locations" }
    ] %>
  </div>

  <div class="border-b pb-4 mb-6">
    <h1 class="text-3xl font-bold">Locations</h1>
  </div>

  <% if @locations.any? %>
    <div class="flex flex-col gap-3">
      <%= render partial: 'pub/shared/list_item', collection: @locations, as: :record %>
    </div>

    <% if @pagy.pages > 1 %>
      <div class="mt-8"><%== pagy_nav(@pagy) %></div>
    <% end %>
  <% else %>
    <p class="text-gray-500 italic">There are no published locations yet.</p>
  <% end %>
</div>
