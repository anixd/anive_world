<div class="p-6">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold"><%= @calendar.name %></h1>
    <div>
      <%= link_to 'Edit', edit_forge_timeline_calendar_path(@calendar), class: 'bg-indigo-500 hover:bg-indigo-600 text-white font-bold py-2 px-4 rounded' %>
      <%= link_to 'Back to List', forge_timeline_calendars_path, class: 'ml-2 text-blue-600' %>
    </div>
  </div>

  <div class="bg-white shadow rounded-lg p-6">
    <div class="prose max-w-none">
      <%= render_markdown(@calendar.description) %>
    </div>
  </div>
</div>
