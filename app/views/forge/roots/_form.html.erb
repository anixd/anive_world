<%= form_with(model: [:forge, @language, @root], class: "space-y-6") do |form| %>
  <%# ... error messages block ... %>

  <div>
    <%= form.label :text, "Root Text", class: "block font-bold mb-1" %>
    <%= form.text_field :text, class: "border rounded w-full p-2" %>
    <p class="text-xs text-gray-600 mt-1">The root itself, e.g., "lar".</p>
  </div>

  <div>
    <%= form.label :meaning, "Meaning", class: "block font-bold mb-1" %>
    <%= form.text_area :meaning, rows: 5, class: "border rounded w-full p-2" %>
    <p class="text-xs text-gray-600 mt-1">Description of the root's meaning. Markdown is supported.</p>
  </div>

  <div class="actions">
    <%= form.submit "Save Root", class: "bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded cursor-pointer" %>
  </div>
<% end %>
