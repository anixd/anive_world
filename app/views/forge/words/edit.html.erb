<div class="p-6">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">Editing the meaning of a word «<%= @word.spelling %>»</h1>
    <p class="text-base text-gray-800 mt-2">Lang: <%= @word.language.name %></p>
  </div>
  <div class="bg-white rounded-lg shadow p-6">
    <%= render 'form', object: [:forge, @word.lexeme.language, @word.lexeme, @word] %>
  </div>
</div>
